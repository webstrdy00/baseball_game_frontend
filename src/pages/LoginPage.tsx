import React from 'react';

const LoginPage: React.FC = () => {
  return (
    <div className="container">
      <h1>로그인</h1>
      {/* 로그인 폼 컴포넌트 추가 예정 */}
    </div>
  );
};

export default LoginPage;